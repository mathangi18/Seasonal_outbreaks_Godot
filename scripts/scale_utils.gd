extends Node
# FEATURES: see /FEATURES_GODOT_FULL.txt, section 3

const PATIENT_RADIUS = 3.0
const PATIENT_BASE_SPEED = 1.8
const WORLD_W = 600
const WORLD_H = 380

func sx(x: float) -> float:
	return x

func sy(y: float) -> float:
	return y

func ss(size: float) -> float:
	return size

func init(viewport, w, h):
	pass
