# Camera & Scale

- Keep sprite sizes modest (128–256 px)
- Use Camera2D with default zoom
- Use scaling helpers when spawning entities
- If sprites appear oversized, check Sprite2D.scale in the Inspector
